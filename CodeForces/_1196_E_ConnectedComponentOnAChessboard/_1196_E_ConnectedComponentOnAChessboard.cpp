#include <bits/stdc++.h>

using namespace std;

int Q;

int main(){
    cin.sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    cin >> Q;
    while(Q--){
        int w, b;
        cin >> w >> b;

    }
}
